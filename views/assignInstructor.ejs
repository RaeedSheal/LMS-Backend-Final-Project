<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Course</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700,900&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tw-elements/dist/css/tw-elements.min.css" />
    <script src="https://cdn.tailwindcss.com/3.3.0"></script>
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
            fontFamily: {
                sans: ["Roboto", "sans-serif"],
                body: ["Roboto", "sans-serif"],
                mono: ["ui-monospace", "monospace"],
            },
            },
            corePlugins: {
            preflight: false,
            },
        };
    </script>
</head>
<body class="bg-gray-200 text-gray-700 h-screen">
    <div class="bg-gray-800">
        <%- include('./partials/Principalheader.ejs') %>
    </div>
    <div class="flex flex-col items-center"> 
        <h1 class="mb-5" >Assign Instructor</h1>
        <form action="/principal/assign/<%= course._id %>" method="post">
            <div class=" w-40 p-6 rounded shadow-sm bg-gray-100">
                <select name="courseInstructor" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 mb-3">
                    <% for(instructor of instructors){ %>
                        <option value="<%= instructor._id %>"><%= instructor.name %></option>
                    <% } %>
                </select>
                <button class="w-full py-2 text-gray-100 bg-blue-700 hover:bg-blue-900 rounded-md transition-colors">Assign</button>
            </div>
        </form>
    </div>
</body>
</html>