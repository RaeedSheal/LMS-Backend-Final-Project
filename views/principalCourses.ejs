<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700,900&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tw-elements/dist/css/tw-elements.min.css" />
    <script src="https://cdn.tailwindcss.com/3.3.0"></script>
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
            fontFamily: {
                sans: ["Roboto", "sans-serif"],
                body: ["Roboto", "sans-serif"],
                mono: ["ui-monospace", "monospace"],
            },
            },
            corePlugins: {
            preflight: false,
            },
        };
    </script>
</head>
<body class="bg-gray-200 text-gray-700 h-screen">
    <div class="bg-gray-800">
        <%- include('./partials/Principalheader.ejs') %>
    </div>
    <div class="flex flex-col items-center"> 
        <h1>Courses</h1>
        <table >
            <thead class="bg-gray-50 border-b-2 border-gray-200 ">
                <tr>
                    <th class="p-3 text-sm tracking-wide text-left">Name</th>
                    <th class="p-3 text-sm tracking-wide text-left">Code</th>
                    <th class="p-3 text-sm tracking-wide text-left">Instructor</th>
                    <th class="p-3 text-sm tracking-wide text-left">Delete</th>
                    <th class="p-3 text-sm tracking-wide text-left">Assign</th>
                </tr>
            </thead>
            <tbody class="bg-gray-100 border-b-2">
                <% for(course of courses){ %>
                    <tr>
                        <td class="p-3 text-sm text-gray-700"><%= course.name %></td>
                        <td class="p-3 text-sm text-gray-700"><%= course.code %></td>
                        <td class="p-3 text-sm text-gray-700">
                            <% if(course.instructor){ %>
                                <%= course.instructor.name %>
                            <% }else{ %>
                                ---
                            <% } %>
                        </td>
                        <td class="p-3 text-sm text-gray-700"><a class="p-1.5 text-xs font-medium uppercase tracking-wider text-red-800 bg-red-500 rounded-lg bg-opacity-50" href="/<%= creator %>/delete/<%= course._id %>"> Delete </a></td>
                        <td class="p-3 text-sm text-gray-700"><a class="p-1.5 text-xs font-medium uppercase tracking-wider text-yellow-800 bg-yellow-500 rounded-lg bg-opacity-50" href="/<%= creator %>/assign/<%= course._id %>"> Assign </a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        
    </div>
    <script src="https://cdn.jsdelivr.net/npm/tw-elements/dist/js/tw-elements.umd.min.js"></script>
</body>
</html>